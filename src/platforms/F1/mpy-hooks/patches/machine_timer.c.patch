--- ../../../../ext/micropython/ports/esp32/machine_timer.c	2025-11-15 20:45:10.589142841 +0100
+++ ./modified_sources/machine_timer.c	2025-11-25 02:29:37.561364598 +0100
@@ -32,6 +32,7 @@
 
 #include "py/mphal.h"
 #include "py/obj.h"
+#include "board_hooks.h"
 #include "py/runtime.h"
 #include "modmachine.h"
 
@@ -117,6 +118,13 @@
 static mp_obj_t machine_timer_make_new(const mp_obj_type_t *type, size_t n_args, size_t n_kw, const mp_obj_t *args) {
     mp_arg_check_num(n_args, n_kw, 1, MP_OBJ_FUN_ARGS_MAX, true);
 
+#ifdef CONFIG_SDK_MPY_HOOK_MACHINE_VIRTUAL_TIMERS_ENABLE
+    // Handle virtual timer (-1)
+    int timer_number_check = mp_obj_get_int(args[0]);
+    if (timer_number_check == -1) {
+        return hook_mpy_machine_timer_virtual_new(type, n_args, n_kw, args);
+    }
+#endif
     // Create the new timer.
     uint32_t timer_number = mp_obj_get_int(args[0]);
     if (timer_number >= SOC_TIMER_GROUP_TOTAL_TIMERS) {
@@ -263,7 +271,17 @@
 }
 
 static mp_obj_t machine_timer_deinit(mp_obj_t self_in) {
-    machine_timer_obj_t *self = self_in;
+#ifdef CONFIG_SDK_MPY_HOOK_MACHINE_VIRTUAL_TIMERS_ENABLE
+    // Check if this is a virtual timer
+    machine_timer_obj_t *self = (machine_timer_obj_t *)self_in;
+    if (self && ((void *)self > (void *)0x1000)) { // Basic pointer validation
+        bool *is_virtual_ptr = (bool *)((uint8_t *)self + sizeof(mp_obj_base_t));
+        if (*is_virtual_ptr) {
+            hook_mpy_machine_timer_virtual_deinit(self_in);
+            return mp_const_none;
+        }
+    }
+#endif
 
     machine_timer_disable(self);
     if (self->handle) {
@@ -276,12 +294,33 @@
 static MP_DEFINE_CONST_FUN_OBJ_1(machine_timer_deinit_obj, machine_timer_deinit);
 
 static mp_obj_t machine_timer_init(size_t n_args, const mp_obj_t *args, mp_map_t *kw_args) {
+#ifdef CONFIG_SDK_MPY_HOOK_MACHINE_VIRTUAL_TIMERS_ENABLE
+    // Check if this is a virtual timer (has is_virtual field at offset 4)
+    machine_timer_obj_t *self = (machine_timer_obj_t *)args[0];
+    if (self && ((void *)self > (void *)0x1000)) { // Basic pointer validation
+        bool *is_virtual_ptr = (bool *)((uint8_t *)self + sizeof(mp_obj_base_t));
+        if (*is_virtual_ptr) {
+            return hook_mpy_machine_timer_virtual_init(n_args, args, kw_args);
+        }
+    }
+#endif
     return machine_timer_init_helper(args[0], n_args - 1, args + 1, kw_args);
 }
 static MP_DEFINE_CONST_FUN_OBJ_KW(machine_timer_init_obj, 1, machine_timer_init);
 
 static mp_obj_t machine_timer_value(mp_obj_t self_in) {
-    machine_timer_obj_t *self = self_in;
+#ifdef CONFIG_SDK_MPY_HOOK_MACHINE_VIRTUAL_TIMERS_ENABLE
+    // Check if this is a virtual timer
+    machine_timer_obj_t *self = (machine_timer_obj_t *)self_in;
+    if (self && ((void *)self > (void *)0x1000)) { // Basic pointer validation
+        bool *is_virtual_ptr = (bool *)((uint8_t *)self + sizeof(mp_obj_base_t));
+        if (*is_virtual_ptr) {
+            uint32_t value = hook_mpy_machine_timer_virtual_value(self_in);
+            return MP_OBJ_NEW_SMALL_INT(value);
+        }
+    }
+#endif
+    
     if (self->handle == NULL) {
         mp_raise_ValueError(MP_ERROR_TEXT("timer not set"));
     }
