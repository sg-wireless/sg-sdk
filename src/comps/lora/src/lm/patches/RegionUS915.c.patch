--- ext/LoRaMac-node/src/mac/region/RegionUS915.c	2026-04-14 16:29:52.190703234 +0200
+++ src/comps/lora/src/lm/modified_sources/RegionUS915.c	2026-05-06 14:39:41.049421794 +0200
@@ -67,7 +67,7 @@
     return txPowerResult;
 }
 
-static bool VerifyRfFreq( uint32_t freq )
+static bool VerifyRfFreq_Rx( uint32_t freq )
 {
     // Check radio driver support
     if( Radio.CheckRfFrequency( freq ) == false )
@@ -82,6 +82,16 @@
     {
         return false;
     }
+    return true;
+}
+
+static bool VerifyRfFreq( uint32_t freq )
+{
+    // Check radio driver support
+    if( Radio.CheckRfFrequency( freq ) == false )
+    {
+        return false;
+    }
 
     // Test for frequency range - take RX and TX freqencies into account
     if( ( freq < 902300000 ) ||  ( freq > 927500000 ) )
@@ -732,7 +742,7 @@
     uint8_t status = 0x07;
 
     // Verify radio frequency
-    if( VerifyRfFreq( rxParamSetupReq->Frequency ) == false )
+    if( VerifyRfFreq_Rx( rxParamSetupReq->Frequency ) == false )
     {
         status &= 0xFE; // Channel frequency KO
     }
@@ -897,7 +907,7 @@
 
 bool RegionUS915ChannelsRemove( ChannelRemoveParams_t* channelRemove  )
 {
-    return LORAMAC_STATUS_PARAMETER_INVALID;
+    return false;
 }
 
 uint8_t RegionUS915ApplyDrOffset( uint8_t downlinkDwellTime, int8_t dr, int8_t drOffset )
